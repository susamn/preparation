[project]
name = "preparation"
version = "0.1.0"
description = "The preparation project for algorithms and system design"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "debugpy>=1.8.19",
]
